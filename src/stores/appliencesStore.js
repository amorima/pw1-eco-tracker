import { defineStore } from 'pinia';

export const useappliencesStore = defineStore('appliences', {
    state: () => ({
        appliences: [],
    }),
    getters: {
        
    },
    actions: {
        
    },
});
